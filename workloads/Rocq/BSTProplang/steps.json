{
  "setup_steps": [
    {
      "Command": {
        "command": "coqc",
        "args": [
          "--print-version"
        ]
      }
    }
  ],
  "build_steps": [
    {
      "Command": {
        "command": "build",
        "run_at": "${workload_path}"
      }
    },
    {
      "Command": {
        "command": "build_generator",
        "run_at": "${workload_path}",
        "args": [
          "!{generator}"
        ]
      }
    }
  ],
  "test_steps": [
    {
      "Command": {
        "command": "${strategy}_sampler.native",
        "run_at": "${workload_path}",
        "args": [
          "${property}",
          "${tests}"
        ]
      }
    }
  ],
  "tags": {
    "generator": [
      "BespokeGenerator",
      "CoverageGenerator"
    ]
  }
}